# flutter-state-management-counter-test

# instalacion
```
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs
```
Una vez esto se puede correr como cualquier proyecto flutter con 
```
flutter run
```

